# 최신 업로드 파일명
- sshctl-v8: 프로그램 코드
- sshctl.completion-v8: 프로그램 bash_completion 코드
- sshctl-개발요구사항-SR_v2.0.0: 개발요구사항 문서
- sshctl-보안설계-SR_v1.2.0: 보안설계 문서


>>> q-20251216-001

현재 업로드한 문서를 기준으로 아래 2개의 문서를 수정해 줘.

---

# 개발 요구사항
현재 문서: sshctl-개발요구사항-SR_v2.0.0

## 변경내용
"10. credential 표시 정책" 내용 변경
- `credential` 처리 규칙 변경
  - `--modify-conneciton | -m` 을 `--list-all | -l`과 동일하게 적용.
    - `--show-credential | -s` 지원

## 개발요구서 작성방법

문서 버전포맷:  SR_v{number}.{number}.{number}

1. 개발요구서(SR_vx.y.z)는 vx.y.z을 기준으로 '추가/수정/삭제'된 내용만 새로 작성하고, 전체 문서를 **다시** 작성한다.
  작성 기준은 아래 내용을 따른다.
  0. x.y.z의 "개요&목차" 구조는 절대적으로 유지한다.
  1. 변경사항이 발생하지 않은 내용은 그대로 유지한다. 
  2. 새로 추가된 부분은 새로운 목록를 추가해서 작성한다.
  3. "13. Changelog" 챕터에 변경사항을 기록한다.
    * "Changelog" 내용은 `<버전> <일자> <시간>` 순서로 내림차순으로 정렬한다.

--- 

# 보안설계
현재 문서: sshctl-보안설계-SR_v1.2.0

## 변경내용
"11.5 credential 표시 정책의 보안 기준 확정" 내용 변경
- `credential` 처리 규칙 변경
  - `--modify-conneciton | -m` 을 `--list-all | -l`과 동일하게 적용.
    - `--show-credential | -s` 지원
## 보안설계문서 작성방법
1. 보안설계문서(SR_vx.y.z)는 vx.y.z을 기준으로 '추가/수정/삭제'된 내용만 새로 작성하고, 전체 문서를 **다시** 작성한다.
  - 보안설계문서(SR_vx.y.z)의 문서구조처럼 "목차(바로가기)"를 작성한다.
    0. vx.y.z 내용을 근거로 최종적으로 결정된 내용을 작성한다.
    1. 변경사항이 발생하지 않은 내용은 그대로 유지한다. 
    2. 새로 추가된 부분은 새로운 목록를 추가해서 작성한다.
  - 마지막 챕터를 "Changelog"로 정하고 변경사항을 기록한다.
    * "Changelog" 내용은 `<버전> <일자> <시간>` 순서로 내림차순으로 정렬한다.
    * 작성예시
    ```
    * `v1.2.0`;2025-12-10 16:54
      * ...
        * ...
          * ...
        * ...
      * ...
    * `v1.1.0`;2025-12-09 12:12
      * .