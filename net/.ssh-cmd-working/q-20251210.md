>>> 개발 진행)
1. 설계/요구사항 정리, ssh-cmd.sh 코드 구현 <---
2. bash_completion 
3. man 페이지/문서화

>>> 코드/개발요구서 작성 요건
1. 코드 수정이 간단한 경우 내가 직접할 수 있도록 
  수정되는 코드의 "수정 전 / 수정 후" 위치와 내용을 명확히 작성해 줘. 
  수정할 코드가 너무 많은 경우는 전체 코드를 제공해 주고.
2. 개발요구서(SR_v1.8.5)는 v1.8.4을 기준으로 '추가/수정/삭제'된 내용만 새로 작성하고,
   작성 기준은 아래와 같아. 
   0. 1.8.4의 "개요&목차" 구조는 절대적으로 유지한다. 
   1. 변경사항이 발생하지 않은 내용은 그대로 유지한다. 
   - 1.8.4 버전에서 변경사항이 발생한 부분은 아래와 같아. 
   - 6. CLI 인터페이스 
   - 7. 목록/미리보기 출력 형식 
   - 10. credential 표시 정책 
   - 11. Changelog 
  2. 새로 추가된 부분은 새로운 목록를 추가해서 작성한다.
>>>
업로드 파일명: ssh-cmd-v3.sh

>>> Q-2025/12/10-001

# 1. 수정사항 
- 'configuration' 데이터 모델
  - 신규 추가
    - masterkey_md5: masterkey 파일의 md5 값. 이 값을 이용해서 설정파일(configuration)과 'open ssh private key'와 정합성을 검증함.
- 'connection' 데이터 모델
  - 이름 변경
    - user -> username
- 보안설계
  - 민감정보: username, credential 
    - host, port 제외

# 2. 신규사항
- 신규 옵션
  - --masterkey: '민감정보' 암/복호화에 사용될 'open ssh private key 파일 경로'
- 전역환경변수 정의
  - SSH_CMD_CONFIG: '연결정보파일 경로'
    - 우선순위
      1. --config 옵션으로 전달되는 값
      2. SSH_CMD_CONFIG 전역변수
      3. ${HOME}/.ssh-cmd/configuration
  - SSM_CMD_MASTERKEY: '민감정보' 암호화/난독화에 사용되는 'open ssh private key'
    - 우선순위
      1. --masterkey 옵션으로 전달되는 값. (--masterkey는 신규 추가 예정)
      2. SSH_CMD_MASTERKEY 전역변수
      3. ${HOME}/.ssh-cmd/masterkey

