#!/usr/bin/env bash

echo
echo "> 가장 최근 커밋과 스테이징도 취소하고, 변경 내용은 유지됩니다."
echo ">  - 최근 커밋과 스테이징 영역을 모두 초기화"
echo ">  - 작업 디렉토리는 그대로 유지"
echo
echo "!!! HEAD~1은 "한 단계 이전 커밋"을 의미합니다. 두 단계 전이면 HEAD~2로 바꾸면 돼요."
echo "!!! 커밋 해시를 직접 지정할 수도 있어요: git reset --soft abc1234"
echo "!!! 이미 원격에 푸시한 커밋을 취소하려면 git push --force가 필요하지만, 팀 작업 중이라면 신중하게 써야 합니다."
echo
echo " git reset --mixed HEAD~1"
git reset --mixed HEAD~1

echo
echo ">>> 현재 상태 >>>"
git status

exit 0

